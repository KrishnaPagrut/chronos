services:
  - type: web
    name: chronos-explorer
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn chronos.demo_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CHRONOS_DEMO_ONLY
        value: "1"
