# Chronos dependencies (6 of the 10-max budget).
# sqlite3 is in the stdlib; MapLibre GL loads from a CDN (no build step).
fastapi==0.139.2
uvicorn==0.51.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.2.2
pytest==9.1.1
